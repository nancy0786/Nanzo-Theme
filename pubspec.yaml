name: nanzo_theme
description: The Ultimate Tablet UI for Lenovo Tab M10 HD.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.32.2"

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.2
  shared_preferences: ^2.2.2
  image: ^4.1.3
  native_device_orientation: ^2.1.0
  permission_handler: ^11.3.0
  flutter_blurhash: ^0.8.2
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

